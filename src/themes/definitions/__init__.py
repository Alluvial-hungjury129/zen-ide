"""
Theme definitions package.
Each theme is defined in its own file.

Themes are lazy-loaded by _LazyThemeDict in theme_definitions.py.
Do NOT import theme constants here — it defeats lazy loading.
"""
